For each review comment below: fix it directly in the codebase if you can, then
list its title under `fixed`. Otherwise — when the issue is environmental, out
of scope, or a false positive — list its title and a brief reason under
`ignored`. Every input issue should appear in exactly one of the two lists.

Prefer minimal, scoped fixes.