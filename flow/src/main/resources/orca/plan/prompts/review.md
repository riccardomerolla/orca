Review the development plan below — which you produced earlier in this session —
and return an improved version of it.

Check for: missing tasks; tasks that should be split or merged; wrong ordering or
unmet dependencies between tasks; vague or ambiguous descriptions; and steps that
don't match how this codebase actually works (you explored it while planning).
Tighten each task description so an implementer could act on it without guessing.

Keep the same epicId unless it is clearly wrong. If the plan includes a Brief
section, refine it in the same spirit; do not invent one if it has none. Return
the complete improved plan, not just the changes.
